I want to create a IntelliJ (PyCharm) Plugins,
which displays me `# end if`s in open python files,
like it is documented in @ai/skills/code-style/references/py.md skill.

The idea is to add this as a virtual element,
displayed only in the IDE to not have other devs be confused by that.
This could be similar to how the parameters of function paramters are having a virtual text annotation in many IntelliJ products/languages.

You may also ask for help or search the web.
Download useful documentation (also) with the `./script/download_ref.py --no-ide-open` tool, which makes them available in the `./ai/references/` folder for later reference.

So the virtual element is not text of the source code, but a thing the IDE adds visually, which looks like the common `# end …`.







